# ADA Panel key state tester

```bash
npm i
npm run start
```
Go to [http://localhost:3000/](http://localhost:3000/)

## Press "Enter" on the ADA panel -> should render key event state in UI

### Possible states:
- `PRESSED`
- `HOLDING`
- `RELEASE`